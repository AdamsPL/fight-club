FIND_PACKAGE(Qt4 COMPONENTS QtCore REQUIRED)

INCLUDE(${QT_USE_FILE})

SET(SRC
	GameEnvironment.cc
	Config.cc
	main.cc
)

SET(HEADERS
)

QT4_WRAP_CPP(HEADERS_MOC ${HEADERS})

ADD_LIBRARY(fc-engine SHARED ${SRC} ${HEADERS_MOC})
TARGET_LINK_LIBRARIES(fc-engine ${QT_LIBRARIES})
